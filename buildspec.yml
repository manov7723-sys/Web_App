version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing Angular CLI"
      - npm install -g @angular/cli

  pre_build:
    commands:
      - echo "Installing frontend dependencies"
      - cd $CODEBUILD_SRC_DIR/frontend && npm install

  build:
    commands:
      - echo "Building Angular frontend"
      - cd $CODEBUILD_SRC_DIR/frontend && ng build --configuration production 2>&1
      - echo "Installing backend dependencies"
      - cd $CODEBUILD_SRC_DIR/backend && npm install --omit=dev

  post_build:
    commands:
      - echo "Build completed successfully"
      - echo "Build timestamp $(date)"

artifacts:
  files:
    - appspec.yml
    - backend/**/*
    - frontend/dist/angular-15-crud/**/*
    - scripts/**/*
  discard-paths: no